name: rotare_app
description: A new Flutter project for Rotare transportation app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.17
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  provider: ^6.0.5
  mapbox_gl: ^0.16.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  firebase_messaging: ^14.6.9
  firebase_core: ^2.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
